# tests

No unit or integration test projects for now — that decision is deliberate, not a gap. This folder
is reserved for end-to-end tests only, driven through Playwright against a running game, added when
there's a real flow worth covering that way (starting around M4's vertical slice).
