# Google OAuth client id (Google Cloud Console -> APIs & Services -> Credentials).
# Public by design - client ids are not secrets. Copy this file to .env.local and fill in the
# real value for local dev; the client falls back to hiding the Google sign-in button if unset.
VITE_GOOGLE_CLIENT_ID=
